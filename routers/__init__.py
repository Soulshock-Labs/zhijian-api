"""
routers — FastAPI APIRouter 路由包

子模块：
  ops          — /public-stats /track-event /feedback /register-lite /bug-report /feedback-feed
  redeem       — /redeem* /partner/*
  user         — /user/*
  template     — /template/* /standard-templates /standard-weekly-*
  generate     — /generate /preview /generate-weekly /generate-daily /generate-observation
"""
